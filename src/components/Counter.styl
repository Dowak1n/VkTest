.counter 
    display: flex;
    align-items: center;
    font-family: roboto, serif;
    font-weight: 500;
    background: rgba(255, 255, 255, 0.12)
    justify-content: center;
    font-size: 15px
    margin-left: 10px
    border-radius: 999px

    &.single-digit
        min-width: 24px
        height: 24px 
        
    &.more-digit
        height: 24px
        padding: 0 6px